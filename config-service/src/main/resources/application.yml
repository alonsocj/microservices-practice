server:
  port: 8081
spring:
  main:
    web-application-type: none
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/alonsocj/microservices-practice
          search-paths: config-data
  application:
    name: config-service